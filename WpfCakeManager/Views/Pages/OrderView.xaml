<Page x:Class="WpfCakeManager.Views.OrderView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfCakeManager.Views"
        xmlns:userControl="clr-namespace:WpfCakeManager.MyUserControl.Single"
        mc:Ignorable="d"
        Title="Gestion commande">
    <Grid Margin="5">
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <userControl:OrderUserControl x:Name="OrderUserControl" Grid.Row="0"/>
        <Button x:Name="ValidateB" Grid.Row="1" Margin="30" FontSize="24">OK</Button>
    </Grid>
</Page>
